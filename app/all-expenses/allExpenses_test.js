'use strict';

describe('expenseTrak.allExpenses module', function() {

  beforeEach(module('expenseTrak.allExpenses'));

  describe('allExpenses controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var allExpensesCtrl = $controller('AllExpensesCtrl');
      expect(allExpensesCtrl).toBeDefined();
    }));

  });
});
